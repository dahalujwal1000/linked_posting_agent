import Link from "next/link";
import { ArrowRight, CheckCircle, Clock, Sparkle, TrendUp, WarningCircle } from "@phosphor-icons/react";
import { AppShell } from "@/components/app-shell";
import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";
import { demoCandidates, demoDrafts } from "@/lib/demo-data";
import { formatDate } from "@/lib/utils";

const statCards = [{ label: "Discovered today", value: "24", detail: "6 strong matches", icon: Sparkle }, { label: "Awaiting review", value: "3", detail: "1 ready to approve", icon: CheckCircle }, { label: "Scheduled", value: "2", detail: "Next: Tue, 9:00 AM", icon: Clock }, { label: "Failed jobs", value: "0", detail: "All systems healthy", icon: WarningCircle }];
export default function Dashboard() { return <AppShell><div className="mx-auto max-w-7xl space-y-7 p-4 sm:p-7"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><h2 className="text-2xl font-bold tracking-tight sm:text-3xl">Good morning</h2><p className="mt-1 max-w-xl text-sm text-[var(--muted)]">Your agent found useful signals. Review the source-grounded drafts before anything gets scheduled.</p></div><Button asChild><Link href="/ideas">Review today’s ideas <ArrowRight size={17}/></Link></Button></div><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{statCards.map(({ label, value, detail, icon: Icon }) => <Card key={label} className="p-4"><div className="flex items-start justify-between"><p className="text-sm text-[var(--muted)]">{label}</p><Icon size={19} className="text-[var(--accent)]"/></div><p className="mt-5 text-3xl font-bold tracking-tight">{value}</p><p className="mt-1 text-xs text-[var(--muted)]">{detail}</p></Card>)}</div><div className="grid gap-5 xl:grid-cols-[1.55fr_1fr]"><Card className="overflow-hidden"><div className="flex items-center justify-between border-b p-5"><div><h3 className="font-semibold">Draft awaiting review</h3><p className="mt-1 text-sm text-[var(--muted)]">Quality score includes source and claim checks.</p></div><span className="rounded-full bg-emerald-500/10 px-2.5 py-1 text-sm font-semibold text-emerald-700 dark:text-emerald-400">{demoDrafts[0].qualityScore}/100</span></div><div className="space-y-4 p-5"><p className="text-sm font-medium">{demoDrafts[0].title}</p><p className="whitespace-pre-line text-sm leading-6 text-[var(--muted)]">{demoDrafts[0].content}</p><div className="flex flex-wrap gap-2"><Button asChild><Link href="/review">Open review</Link></Button><Button variant="outline" asChild><Link href="/drafts">Edit draft</Link></Button></div></div></Card><Card className="p-5"><div className="flex items-center justify-between"><h3 className="font-semibold">Pillar balance</h3><TrendUp size={20} className="text-[var(--accent)]"/></div><p className="mt-1 text-sm text-[var(--muted)]">This week’s planned mix</p><div className="mt-6 space-y-4">{[["Projects",35],["Education",30],["Industry",20],["Career",15]].map(([name, value]) => <div key={String(name)}><div className="mb-1.5 flex justify-between text-sm"><span>{name}</span><span className="text-[var(--muted)]">{value}%</span></div><div className="h-2 overflow-hidden rounded-full bg-black/5 dark:bg-white/10"><div className="h-full rounded-full bg-[var(--accent)]" style={{ width: `${value}%` }}/></div></div>)}</div></Card></div><Card><div className="flex items-center justify-between border-b p-5"><div><h3 className="font-semibold">Top candidates</h3><p className="mt-1 text-sm text-[var(--muted)]">Ranked by your configured scoring rules.</p></div><Link href="/ideas" className="text-sm font-semibold text-[var(--accent)]">All ideas</Link></div><div className="divide-y">{demoCandidates.map(c => <div className="flex flex-col gap-2 p-4 sm:flex-row sm:items-center sm:justify-between" key={c.id}><div><p className="font-medium">{c.title}</p><p className="mt-1 text-sm text-[var(--muted)]">{c.source} · {formatDate(c.publishedAt)} · {c.topic}</p></div><span className="w-fit rounded-full bg-[var(--accent)]/10 px-2.5 py-1 text-sm font-semibold text-[var(--accent)]">{c.totalScore}/100</span></div>)}</div></Card></div></AppShell>; }
